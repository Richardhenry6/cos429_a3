function drawFaceSequence(fs, from, step, number, rects)
% drawFaceSequence  
%
% drawFaceSequence(fs, from, step, number, rects)
%fill in here  
  for i = from:step:number
    co = imageco(fs.read(i));
    if(nargin>=5)
      rectDraw(rex);
    end
    pause(0.2);
  end
end
