function motinv = uvsInv(mot)
% uvsInv  invert the transScaleUV motion 
%
% motinv = uvsInv(mot)
%
% motinv is defined such that: 
%    uvsPlus(mot,motinv) -> zero motion with center at mot's center;

%Fill in here
% enum
U = 1;
V = 2;
S = 3;
X0 = 4;
Y0 = 5; 
zero_motion = [ 0 0 0 mot(:,X0) mot(:,Y0)] 
newcenter = uvsCWarp(mot, [mot(:,X0),mot(:,Y0)]); 
%motinv = uvsChangeCenter(mot, newcenter); 
%params
motinv = [-mot(:,U) -mot(:,V) -mot(:,S) newcenter];  